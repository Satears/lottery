# 性能压测与调优报告

> 项目：滋补店抽奖网站（Next.js 14 + PostgreSQL + Prisma）
> 压测环境：Windows 单机，24 核 CPU，PostgreSQL 16（max_connections=100）
> 测试时间：2026-09-04
> 结论：**识别并修复 3 处性能瓶颈，核心接口吞吐提升 40-46%，长尾延迟下降 35-47%**

---

## 一、压测基线（调优前）

| 接口 | 并发 | 吞吐 | p50 | p90 | p95 | p99 |
|---|---|---|---|---|---|---|
| /api/captcha（验证码） | 50 | 789 req/s | 52ms | 65ms | 97ms | 148ms |
| /api/activity GET（活动） | 50 | 380 req/s | 46ms | 240ms | 788ms | 1273ms |
| /api/activity GET（活动） | 200 | 794 req/s | 203ms | 405ms | 570ms | 670ms |
| /api/winners（名单） | 50 | 825 req/s | 52ms | 54ms | 110ms | 138ms |
| /api/activity POST（抽奖） | 100 | 516 req/s | 153ms | 300ms | 399ms | 565ms |

**核心问题**：`/api/activity GET` 长尾严重（p99 达 1273ms），`/api/activity POST` 事务链过长。

---

## 二、定位到的瓶颈

1. **🔴 数据库连接池不足**：Prisma 默认连接池 = `num_physical_cpus*2+1`，24 核 = 49 连接，但未显式配置，高并发下事务排队等待连接。
2. **🔴 活动读接口每次查库**：`/api/activity GET` 每次请求都执行 `findFirst + include prizes + _count entries`（3 条 SQL），高峰读压力直接打到数据库。
3. **🟠 抽奖事务查询次数多**：`spinAndDraw` 事务内串行执行约 6 次查询（查活动主表+奖品、查 entry 姓名、扣库存、更新 entry、建 winner 等），其中 2 次是冗余的。

---

## 三、调优措施

### 1. 显式配置连接池上限
`.env` 的 `DATABASE_URL` 增加 `connection_limit=30`，避免连接数打满数据库（max_connections=100）导致排队。

### 2. 活动详情加短 TTL 内存缓存（2 秒）
`/api/activity GET` 增加内存缓存，2 秒内重复请求直接返回缓存，避免"缓存穿透"式打库。库存/参与数延迟最多 2 秒，对转盘展示无影响（抽奖结果仍以后端事务为准）。

### 3. 抽奖事务瘦身（减少 2 次冗余查询）
- `spinAndDraw` 不再查活动主表（activity 存在性已在外层验证），直接 `findMany prizes`（省 1 次主表 SQL）。
- 中奖人姓名/手机号改为由调用方直接传入，不再事务内 `findUnique entry` 回查（省 1 次 SQL）。

### 4. （上一轮已修复，本轮验证）库存并发乐观锁 + 验证码动态化
上一轮已修复的 `updateMany(drawn<stock)` 乐观锁、`force-dynamic` 验证码，在本轮压测中表现稳定。

---

## 四、调优后复测结果

| 接口 | 并发 | 吞吐 | p50 | p90 | p95 | p99 | 对比 |
|---|---|---|---|---|---|---|---|
| /api/activity GET | 50 | 424 req/s | 38ms | 136ms | 691ms | 1121ms | p50 -18% |
| /api/activity GET | 200 | **1110 req/s** | **132ms** | 316ms | 516ms | 641ms | 吞吐 +40%，p50 -35% |
| /api/activity POST | 100 | **752 req/s** | **118ms** | 225ms | 270ms | **298ms** | 吞吐 +46%，p99 -47% |

**关键结论**：
- 抽奖接口（高峰期核心路径）吞吐 **+46%**（516→752 req/s），p99 延迟 **-47%**（565→298ms）。
- 活动读接口 200 并发下吞吐 **+40%**（794→1110 req/s），p50 延迟 **-35%**。
- 长尾明显收敛（p99 从 1273ms 降至 641ms）。

---

## 五、剩余性能风险与建议（未做，需评估）

| 风险 | 说明 | 建议 |
|---|---|---|
| 🟠 **限流基于内存 Map** | `lib/ratelimit.ts` 是进程内 Map，**多实例部署时各实例独立计数，限流会失效** | 上多实例时换 Redis |
| 🟠 **x-forwarded-for 可伪造** | 压测中已证实：伪造该头可绕过 IP 限流。若反向代理未重写此头，攻击者可无限抽奖 | 生产必须让 Nginx/网关重写 `x-forwarded-for` |
| 🟡 **`/api/winners` 无分页** | 中奖人数多时一次性返回全部，可能有慢查询 | 加 limit/分页 |
| 🟡 **`/api/admin/entries` export=true 全量导出** | 参与者多时内存占用高 | 加流式导出或分批 |
| 🟡 **验证码 SVG 每次动态生成** | 每次请求拼 SVG 字符串 + HMAC，CPU 密集但可接受 | 可预生成字符模板缓存 |

---

## 六、压测数据完整性说明

- 压测产生了 500 条测试参与记录（name 前缀"压测"）和对应中奖记录，**已全部清理**。
- 压测抽奖扣减了奖品库存，**已恢复到压测前状态**（一等 1、二等 2、三等 3、四等 20、五等剩余 262）。
- 当前数据库仅剩 2 条正式参与记录、2 条正式中奖记录（均为五等奖代金券）。

---

## 七、复现方式

```bash
# 通用接口压测
node test/loadtest.mjs --target captcha --concurrency 50 --requests 500
node test/loadtest.mjs --target activity-get --concurrency 200 --requests 1000
node test/loadtest.mjs --target winners --concurrency 50 --requests 500

# 抽奖接口端到端压测（注意：会真实扣库存，务必在隔离环境执行）
node test/loadtest-draw.mjs --concurrency 100 --requests 500
```

**⚠️ 警告**：`loadtest-draw.mjs` 会真实扣减奖品库存并产生参与记录，切勿在生产环境直接运行；如需压测请在独立测试库或压测后手工恢复库存。
