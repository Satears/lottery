# 生产部署就绪检查报告

> 项目：滋补店抽奖网站（Next.js 14 + PostgreSQL + Prisma）
> 检查时间：2026-09-04
> 结论：**已修复 4 个生产级问题，可进入部署阶段（仍有 2 项上线前必做项）**

---

## 一、已修复的问题（本次检查中发现并修复）

### 🔴 P0-1：验证码被静态化（致命）
- **现象**：`/api/captcha` 被 Next.js 标记为 `○ Static`，`Math.random()` 只在构建时执行一次，导致**连续多次请求返回完全相同的验证码**（实测 3 次都是 `8319`）。
- **影响**：验证码形同虚设，任何人都能用固定值绕过，可被脚本批量刷奖。
- **修复**：在 `app/api/captcha/route.ts` 加 `export const dynamic = "force-dynamic"`。同时对 `activity`、`winners` 两个动态公开接口也补了同样的声明。
- **验证**：重启后连续 3 次验证码为 `6056 / 9334 / 1997`，已动态。

### 🔴 P0-2：抽奖库存并发超发
- **现象**：`lib/spin.ts` 中"检查 `drawn < stock`"和"`drawn + 1`"之间没有原子保护，并发请求可能都读到 `drawn = stock-1` 然后都加一，导致**库存超发**（例如一等奖只有 1 份却可能发出 2 份）。
- **修复**：改用条件更新 `updateMany({ where: { id, drawn: { lt: stock } }, data: { drawn: { increment: 1 } } })`，`count === 0` 时回退为"谢谢参与"。
- **影响面**：一等奖（stock=1）尤其脆弱，修复后并发下也不会超发。

### 🟠 P1-3：收货地址可被越权篡改
- **现象**：`/api/submit-address` 只校验 `entryId` 存在且中奖，但 `entryId` 是自增整数，可被枚举去改**别人**的收货地址。
- **修复**：新增姓名+手机号双重校验，必须与参与记录一致（`403` 拒绝）。前端 `app/page.tsx` 提交时同步带上 `name`/`phone`。

### 🟠 P1-4：中奖查询接口可被枚举
- **现象**：`/api/check-winner` 无频率限制，可用手机号字典批量探测中奖信息。
- **修复**：加 IP 限流（60 秒 10 次）。

### 🟡 P2-5：生产用 `db push` 而非迁移
- **现象**：Dockerfile / railway.json 启动命令用 `prisma db push`（开发用命令，可能覆盖/破坏数据）。
- **修复**：生成基线迁移 `prisma/migrations/20260904150000_init/migration.sql`，部署命令改为 `prisma migrate deploy`；`package.json` 增加 `db:migrate` 脚本；本地已 `migrate resolve --applied` 标记。

---

## 二、上线前必须完成（2 项）

| # | 事项 | 说明 |
|---|---|---|
| 1 | **改后台密码** | 当前 `ADMIN_PASSWORD=admin123456`，登录接口 `/api/admin/login` 返回明文 token（等于密码）。上线必须改成强密码，并建议后续改为 JWT/会话 token。 |
| 2 | **设置 CAPTCHA_SECRET** | 验证码 HMAC 密钥当前用默认值 `lucky-draw-captcha`。**多实例部署时所有实例必须一致**，且必须改成随机值。已在 `.env.example` 补充说明。 |

---

## 三、其余发现（建议，非阻断）

- **无 Git 仓库**：`git status` 报 `not a git repository`。强烈建议 `git init` + 提交，防止代码/测试脚本意外丢失（历史中 run-tests.mjs 曾丢失过）。
- **限流是内存实现**（`lib/ratelimit.ts` Map）：单实例有效，多实例部署时各实例独立计数，限流失效。若上多实例需换 Redis。
- **管理端登录用 localStorage token**：前端把 token 存在 localStorage，有 XSS 泄露风险；且后端 token 即明文密码。建议改 HttpOnly Cookie 会话。
- **`/api/admin/login` 无失败限流**：可暴力破解后台密码。建议加 IP 限流。
- **`getIp` 信任 `x-forwarded-for`**：若未正确配置反向代理，可被伪造 IP 绕过限流。上线需确保代理层重写该头。
- **`.env` 已正确 gitignore**，`.dockerignore` 也排除了 `.env`，但需确认 Railway 面板里配了完整环境变量（含 CAPTCHA_SECRET）。

---

## 四、构建与运行验证结果

- ✅ `next build` 通过，17 路由全部编译成功，无 TypeScript 错误
- ✅ `/api/captcha` 已从 `○ Static` 变为 `ƒ Dynamic`
- ✅ 服务运行于 `http://localhost:3001`，首页 200
- ✅ 数据库仅剩正式活动 #1（`滋补好礼 · 幸运转盘`，ACTIVE），残留测试活动已清理
- ✅ 抽奖 logo 与奖品映射 5/5 正确（上一轮已验证）

---

## 五、部署步骤（Railway / Docker）

1. 在 Railway（或目标平台）配置环境变量：`DATABASE_URL`、`ADMIN_PASSWORD`（强密码）、`CAPTCHA_SECRET`（随机）
2. 推送代码（先 `git init` + 提交）
3. 构建时自动 `prisma generate && next build`，启动时自动 `prisma migrate deploy` 建表
4. 首次部署后可运行 `npm run db:seed` 初始化活动（注意：seed 会再建一个活动，若已有 #1 请勿重复执行）
